services:
  api:
    build: .
    ports:
    - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis
      - qdrant

  redis:
    image: redis:7-alpine

  qdrant:
    image: qdrant/qdrant:v1.12.4