# backend/auth/__init__.py
#
# Auth package.
#
# Phase 11 will build full JWT + RBAC here.
# Phase 3 contributes: dependencies.py — the API key dependency used by
# all read endpoints. This is the injection point that Phase 11 replaces.